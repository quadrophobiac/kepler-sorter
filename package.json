{
  "name": "application-name",
  "version": "0.0.1",

  "devDependencies": {"http-server": "^0.6.1"},

  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server --cors -a 'secret-reaches-4650.herokuapp.com' -p 8000 -c-1"

  }
}
